using System;

namespace Selenium.Core.Browsers
{
    [Serializable]
    public enum BrowserType
    {
        Firefox,
        Chrome,
        Safari,
        Explorer,
        Edge,

        Remote
    }
}